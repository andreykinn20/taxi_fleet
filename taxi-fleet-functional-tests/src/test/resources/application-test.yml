spring:
  datasource:
    url: jdbc:mariadb://${embedded.mariadb.host}:${embedded.mariadb.port}/${embedded.mariadb.schema}?createDatabaseIfNotExist=true&nullNamePatternMatchesAll=true&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8
    username: ${embedded.mariadb.user}
    password: ${embedded.mariadb.password}
  liquibase:
    contexts: main,test